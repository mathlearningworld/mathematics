export interface BridgeCrossingUiState {
  readonly showApproachMarker: boolean;
  readonly showObservationMarker: boolean;
  readonly showPreparationMarker: boolean;
  readonly showBlockedPath: boolean;
  readonly showCrossablePath: boolean;
  readonly showDestinationMarker: boolean;
  readonly showCompletion: boolean;
  readonly highlightBridge: boolean;
}
